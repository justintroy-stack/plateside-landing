# Plateside — early access landing page

The pre-launch demand-validation page for Plateside, served at plateside.app.
Static HTML only — no build step, no user data collected here beyond what a
visitor explicitly submits through the email form on the page.

The Plateside app itself lives in a separate repository.
